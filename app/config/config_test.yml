imports:
    - { resource: config_dev.yml }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

swiftmailer:
    disable_delivery: false
    delivery_addresses: ['%devtest_delivery_addresses%']

parameters:
    app.cir_env: 'TEST'
